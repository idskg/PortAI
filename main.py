import whisper
import sounddevice as sd

def main():
    if (__name__ == "__main__"):

        model = whisper.load_model("medium") # Load the whisper model before doing any dumb stuff
        # We need to check for an available nvidia gpu
main()